# v2.61.14-experimental

## 实验性更新

- 编辑器工具栏新增收起按钮（左侧下箭头），点击后工具栏隐藏且键盘弹出不再自动显示
- 右上角模板按钮替换为编辑器显示/隐藏切换（眼睛图标），可随时恢复工具栏
- 标签区域支持折叠/展开，折叠后以小图标形式显示已选标签
- 编辑器正文区域添加水平参考线，辅助光标定位，行距随字号自动调整

## 验证

- `:app:compileExperimentalDebugKotlin` 通过
- `:app:assembleExperimentalRelease` 通过
