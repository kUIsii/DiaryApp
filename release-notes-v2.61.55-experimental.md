## 修复

- 修复日记详情预览把连续文本误显示成“段落空一行”的问题，保留真正的空行但不再给普通换行额外加间距。
- 重做备份入口为统一的“备份管理”：现在可以在同一页面内执行本地备份、导入备份、重命名备份和删除备份。
- 调整编辑器光标定位逻辑，改为按 WebView 实际可视区域对齐，减少随机跳到较远行时被键盘或工具区遮挡的问题。
- 退出未保存编辑时新增“保存并退出”，不需要再先返回上一级再手动保存。

## 说明

- 本次为 `experimental` 渠道功能与交互修复更新。
- APK 会随 release 一并发布。
