# v2.61.16-experimental

本版本修正 v2.61.15 实验包中编辑页 UI 落地不完整的问题：

- 将真实 App 编辑页进一步对齐 `editor-ui-preview.html`：轻顶栏、无边框标题、贴近标题的元信息条、纸张式正文区域、底部轻工具栏。
- 修复正文 WebView 被挤成小滚动区域的观感：编辑器外层改为纸张容器并占满剩余页面空间。
- 修复横线和文字行距错位：横线起点随正文 padding 对齐，并继续随字号自动调整。
- 折叠元信息不再是零散小图标，改为一条紧凑 pill strip；展开后保留心情、天气、标签、位置的完整入口。
- 底部工具栏改为页面流内的轻量工具条，减少遮挡和额外预留空间。
- 降低编辑器底部预留高度，避免正文可写区域浪费。

草稿安全逻辑沿用上一版修复：自动保存不再清掉未保存状态，已有日记自动草稿可提示恢复，关闭恢复弹窗不再直接删除草稿。
