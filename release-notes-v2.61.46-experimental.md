## 新增

- 首页右上角功能菜单新增 `实验功能` 入口
- 新增独立的 `实验功能` 页面，可单独打开或关闭测试中的功能

## 当前实验功能

- `主页面左右滑动切换`
  - 可在 `首页 / 时间线 / 待办 / 我的` 之间左右滑动切换
  - 待办页内部原有的横向切换仍然保留，滑到边界时才切换主页面

- `完成项保留原位置`
  - 待办和备忘中勾选完成后，不再自动移动到底部
  - 会保留在原来的位置，仅显示划线和完成状态

## 说明

- 实验功能默认关闭
- 一旦手动开关，状态会保留，下次进入 App 仍然生效
