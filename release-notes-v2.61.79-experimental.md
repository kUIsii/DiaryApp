## 修复

- 修复部分设备上插入图片后编辑器没有任何反应的问题。
- 图片选择改为更通用的 `image/*` 内容选择器，减少相册来源兼容性问题。
- 图片导入失败或编辑器插入失败时会给出提示，不再静默失败。
- 当图片选择返回时 WebView 编辑器尚未准备好，会先缓存待插入图片，编辑器 ready 后再自动插入。
- 编辑器侧 `insertMedia()` 现在会返回插入成功或失败，便于 Android 侧判断结果。

## 说明

- 本次为 `experimental` 渠道补丁更新。
- 版本号已提升至 `2.61.79-experimental`，用于确保能检测到更新。
- 当前 Kotlin 编译和 experimental release APK 构建已通过。
