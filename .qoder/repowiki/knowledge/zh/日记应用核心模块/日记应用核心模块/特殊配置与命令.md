- **API 密钥配置**：需在项目根目录 `local.properties` 中配置 `GITHUB_TOKEN` (用于更新检查) 和 `AMAP_API_KEY` (用于地图服务)，构建脚本会自动将其注入 `BuildConfig` 和 `AndroidManifest`。
- **产品风味**：支持 `stable` (稳定版) 和 `experimental` (实验版) 两种风味，拥有不同的 Application ID 和版本号策略。