## 新增

- 新增首页右上角「媒体库」入口，可集中查看过往日记中的所有图片。
- 媒体库使用缩略图网格展示，点击图片可直接跳转到对应日记详情，后续可继续扩展为回忆墙和时间线联动。

## 修复与优化

- 修复统计页热力图只显示单一颜色的问题，现在同一天多篇日记会正确参与颜色挡位计算。
- 修复日记图片插入后在编辑器和详情页显示为小图标的问题，统一通过 App 私有媒体目录和 WebView AssetLoader 加载。
- 插入图片时会压缩生成展示图和缩略图，避免大图直接写入日记正文导致内存溢出。
- 新增稳定的 `diary-media://` 图片引用，保存后的日记不再依赖相册原图是否还存在。
- 备份升级为 `.diarybackup` 包，包含日记 JSON 和已引用的日记图片；旧 JSON 备份仍可导入。

## 说明

- 本次为 `experimental` 渠道功能更新。
- 版本号已提升至 `2.61.78-experimental`，用于确保能检测到更新。
- 当前 Kotlin 主代码与测试代码编译已通过；本机完整单元测试执行仍受 Gradle Test Executor 环境异常影响。
